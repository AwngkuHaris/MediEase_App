import 'package:flutter/material.dart';

class HealthEdPage extends StatelessWidget {
  const HealthEdPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("ini healthedpage"),
    );
  }
}